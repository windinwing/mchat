# MChat — 以 Skill 编排为核心的垂直 RAG 平台

> 在线体验：<https://mchat.9235.net> · 开源协议：MIT

MChat 把**技能（Skill）**当作最小积木，用**可视化工作流**把它们编排成可复用的自动化流水线——不是一次性的对话，而是「检索 → 并行分析 → 汇总 → 出报告」的多步、可调度、可触达多渠道的真实业务流程。

---

## 一句话定位

**一个 Skill 是一个工具，一条 Workflow 是一组工具的编排。**

MChat 在同一个栈里打通了：流式 Bot 引擎、生产级 RAG 知识库、热加载 Skill 插件、可视化 Workflow 编排，以及一行 `<script>` 就能嵌入任意网站的 Widget。

---

## Skill 编排：把工具串成流水线

这是 MChat 最核心的能力。在 **管理后台 → 工作流**（`/admin/workflows`）里，用 ComfyUI 风格的画布拖拽节点，就能把多个 Skill 组合成 DAG：

![工作流图编辑器：专利多维度分析的 Skill 编排](https://mchat.9235.net/landing/workflow.graph.cn.png)

上图是一条真实的「专利多维度分析」工作流：`开始 → 专利检索` 之后**分叉成 4 个并行 Skill**（按申请人 / 年份 / 公司 / 综合强度分析）→ `MERGE 汇总` → `图表绘制` → `报告导出` → `结束`。一条流程同时调用 6+ 个技能节点，每个节点可以配置不同的入参。

### 编排能做什么

| 能力 | 说明 |
|------|------|
| **图形化 DAG** | 指针/平移切换，从技能库拖拽节点；节点类型：`start` · `skill` · `condition` · `approval` · `merge` · `end` |
| **并行 & 汇总** | 多分支并行执行，`merge` 节点等待全部完成后喂给下游 |
| **参数映射** | 节点间用 `${input.keyword}`、`${nodes.<id>.result.xxx}` 模板传递结果 |
| **人工审批** | `approval` 节点暂停流程，等运营在后台通过/驳回 |
| **多种触发** | 手动执行、定时调度（cron）、频道消息触发（微信/Web） |
| **一键复用** | 任意工作流可存为「我的模板」，按 `skill_name` 绑定，跨租户即装即用 |

> 同一个 Skill（如 `patent-search`）能在一条流程里出现多次：一次 `command: search` 做检索，多次 `command: analysis` + 不同 `dimension` 做并行分析——这正是「编排」相对「单次调用」的价值。

---

## 技能（Skill）：可热加载、可分发的积木

![技能管理：热加载、安装、调度](https://mchat.9235.net/landing/mchat.skill.zh.png)

Skill 是最小执行单元——一个工具、函数或 webhook。MChat 支持：

- 从磁盘 / zip / URL **热加载** `SKILL.md`，兼容 OpenClaw 格式
- 后台一键安装技能包（如 `patent-search`、`stock-search`、`web-fetch`）
- 技能定时任务、多语言显示名、付费技能包作为垂直通道增值能力

---

## 完整能力栈

编排只是其中一环，MChat 还内置了让它真正能落地的全套能力：

**知识库（RAG）** —— 多策略分块、混合检索（向量 + BM25 + RRF）、重排序、查询改写、Parent-Child 上下文：

![知识库管理](https://mchat.9235.net/landing/mchat.knowledge.zh.png)

**垂直通道（Agent）** —— 每个通道独立的模型、Prompt、知识库、技能包与 Widget 品牌，数据完全隔离：

![客服 Agent 配置](https://mchat.9235.net/landing/mchat.customer.zh.png)

**嵌入式 Widget** —— 一行 `<script>` 把品牌化 RAG 对话窗口接入任意网站：

![Widget 聊天界面](https://mchat.9235.net/landing/mchat.chat.zh.png)

---

## 工作流管理

工作流列表、模板库、运行记录都在同一个后台管理：

![工作流列表与模板](https://mchat.9235.net/landing/workflow.cn.png)

---

## 技术栈

- **后端**：FastAPI · SQLAlchemy 2.0 · Milvus · 支持 10+ 大模型（OpenAI / Anthropic / Google / DeepSeek / Ollama / Groq …）
- **前端**：React 19 · TypeScript · Vite · Tailwind CSS 4 · Zustand · React Flow
- **部署**：Docker Compose，`make docker-up-lite` 一键起全套

---

## 快速开始

```bash
git clone https://github.com/windinwing/mchat.git
cd mchat
make docker-up-lite

# 管理后台：http://localhost:5173/admin  （默认 admin / admin123）
# API 文档：http://localhost:3001/docs
```

装几个技能 → 打开 **工作流** → 用模板或自己画一张图 → **执行**，就能看到 Skill 编排跑起来。

---

📖 更完整的文档见 [README.zh.md](README.zh.md) / [README.md](README.md)。
